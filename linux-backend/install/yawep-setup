#!/usr/bin/bash

servicos=(empty-files httpd proftpd dovecot postgresql mariadb postfix rabbitmq-server firewalld selinux mod_ssl yawep)

for s in "${servicos[@]}"
do
    echo "Setting up ${s}\n"
    ( cd /usr/share/yawep/setup ; bash setup-$s )
done



