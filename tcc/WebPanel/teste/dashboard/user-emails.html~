<div ng-app="app" ng-controller="EmailsCtrl">
    <form class="well">
        <label>Name</label>
        <input type="text" name="name" ng-model="newdomain.name" />
        <input type="hidden" ng-model="newdomain.id" />        
        <br/>	
        <input type="button" value="Save" ng-click="saveDomain()" class="btn btn-primary" />
    </form>
    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Domain Name</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <tr ng-repeat="domain in domains">
                <td>{{ domain.name }}</td>
                <td> <a href="javascript:void(0)" ng-click="edit(domain.id)">edit</a> | <a href="javascript:void(0)" ng-click="delete(domain.id)">delete</a>

                </td>
            </tr>
        </tbody>
    </table>
</div>
